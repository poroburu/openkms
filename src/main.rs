//! openKMS CLI entry point. Real subcommands land in a later commit; this
//! scaffold stub lets the crate build while each module is introduced in
//! isolation.

fn main() {
    println!("openkms WIP — run `cargo check` while modules land; see README for the plan.");
}
